#include<stdio.h>
#include<conio.h>
void main()
{
      int a,*b,i;
      int mul=1,*fac;
      clrscr();
      printf("\nEnter the number : ");
      scanf("%d",&a);
      b=&a;
      fac=&mul;
      for(i=1;i<=*b;i++)
      {
	    *fac = *fac * i ;
      }
      printf("\n%d is factorial...",mul);
      getch();

}