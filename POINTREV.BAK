#include<stdio.h>
#include<conio.h>
void main()
{
  int num,i,rev=0,ld;
  int *n,*r,*l;
  clrscr();
  printf("Enter your number:");
  scanf("%d",&num);
  n=&num;
  r=&rev;
  l=&ld;
  while(num!=0)
  {
	  ld=num%10;
	  rev=(rev*10)+ld;
	  num=num/10;

  }

     printf("\n%d",rev);
     getch();
}

