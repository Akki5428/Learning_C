#include<stdio.h>
#include<conio.h>
struct date
{
   int year,day,month;
}d;

int scan()
{
     printf("\nEnter your date:");
     scanf("%d %d %d",&d.day,&d.month,&d.year);
}
int validdate(struct date d)
{
      if(t.month==2)
      {
	      if(t.year%4==0)
	      {
		 return 1;
	      }
	      else
	      {
		  printf("%d %d %d - %d  is not a leap year",d.day,d.month,d.year,d.year);
	      }
      }
      if(t.month==1 || t.month==3 || t.month==5 || t.month==7 || t.month==8 || t.month==10 || t.month==12)
      {
	   if(t.date>=1 &&t. date<=31)
	   {
		 return 1;
	   }
	   else
	   {
		  if(t.month==1)
		  {
			  printf("\nJanuary has 31 days");
		  }
		  if(t.month==3)
		  {
			  printf("\nMarch has 31 days");
		  }
		  if(t.month==5)
		  {
			  printf("\nMay has 31 days");
		  }
		  if(t.month==7)
		  {
			  printf("\nJuly has 31 days");
		  }
		  if(t.month==8)
		  {
			  printf("\nAugast has 31 days");
		  }
		  if(t.month==10)
		  {
			  printf("\nOctober has 31 days");
		  }
		  if(t.month==12)
		  {
			  printf("\nNovember has 31 days");
		  }
	   }

      }
}
void main()
{
      int t;
      clrscr();
      scan();
      t=scan();

}
